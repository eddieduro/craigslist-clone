import { showSubcat } from 'craigslist-clone/helpers/show-subcat';
import { module, test } from 'qunit';

module('Unit | Helper | show subcat');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = showSubcat([42]);
  assert.ok(result);
});
